# Local dev: leave unset — Vite proxies /api → http://localhost:8000 automatically
# Railway production: set to your backend Railway URL (no trailing slash)
VITE_API_URL=https://musara-backend.up.railway.app
